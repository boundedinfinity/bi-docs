<script lang="ts">
    // import superjson from "superjson";
    // import config from "../../config/load";
    // import { stores } from "@sapper/app";

    // const { page } = stores();
    // import { Find, ByPath, Breadcrumbs } from "../../config/walk";

    // console.log(config.navigation)

    // $: console.log(`$page.path: ${$page.path}`);
    // $: console.log(Find(config.navigation, ByPath($page.path)));
    // $: {
    //     const bc = Breadcrumbs(config.navigation, $page.path);
    //     console.log(`bc.length: ${bc.length}`);
    // }
</script>
